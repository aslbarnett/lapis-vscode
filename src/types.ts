export type BasePalette = {
  backgroundDark: string;
  background: string;
  foreground: string;
  selectionDark: string;
  selection: string;
  textDark: string;
  textMid: string;
  text: string;
  shadow: string;
};

export type ColorPalette = {
  lapis: string;
  amethyst: string;
  ruby: string;
  amber: string;
  peridot: string;
  aquamarine: string;
  quartz: string;
};
